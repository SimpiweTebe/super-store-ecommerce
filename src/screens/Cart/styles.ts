import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    paddingVertical: 15,
    backgroundColor: '#fff',
    flex: 1
  },
  cartList: {
    gap: 20
  },
  cartActions: {
    backgroundColor: '#fff'
  }
})

export default styles