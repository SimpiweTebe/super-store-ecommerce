import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  carousel: {
    backgroundColor: 'red',
    height: 250,
    marginVertical: 20,
    borderRadius: 15
  }
})

export default styles